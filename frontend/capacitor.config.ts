import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.athleticintel.blindside',
  appName: 'Blindside Dynasty',
  webDir: 'out'
};

export default config;
